import { IDLE_PATHS } from '../../Idle/routes/paths'
import { LOGIN_PATHS } from '../../Login/routes/paths'

export const APPBAR_TITLES: Record<string, string> = {
	// IDLE
	[IDLE_PATHS.HOME]: 'Inicio',

	// LOGIN
	[LOGIN_PATHS.LOGIN]: 'Iniciar sesión',
}
